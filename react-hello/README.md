# Simple sample of using React with Browserify

Run `npm install` to install React from npm. Then run:

```sh
$ npm start
```

[Browserify](http://browserify.org/) is needed to run. To install browserify, run below:

```sh
$ npm install -g browserify
```
